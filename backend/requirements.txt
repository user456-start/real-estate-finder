# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
geoalchemy2==0.15.2        # PostGIS geometry types in SQLAlchemy
pgvector==0.3.6            # pgvector type + operators in SQLAlchemy

# Redis (Places API response cache)
redis==5.2.0

# LLM & agents (Ollama — local, free)
ollama==0.4.4
langchain==0.3.9
langchain-ollama==0.2.3    # ChatOllama for LangGraph nodes
langgraph==0.2.53

# Embeddings handled by Ollama (nomic-embed-text) — no OpenAI key needed

# HTTP client (scrapers + external APIs)
httpx==0.27.2
beautifulsoup4==4.12.3

# Email
sendgrid==6.11.0
jinja2==3.1.4

# Scheduling
apscheduler==3.10.4

# Config / validation
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1

# Observability — installed from local path, not PyPI
# pip install -e /root/electronics/sdk
# llm-observer is listed here as a comment so it's documented,
# but must be installed manually (see README).

# Dev / testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx                      # also used as AsyncClient in tests
